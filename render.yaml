services:
  - type: web
    name: iptv-tuner
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python railway-deploy.py
    envVars:
      - key: PORT
        value: 10000
